# 🚀 GitHub Pages 部署指南

## 📋 部署步骤

### 方法1：GitHub Actions自动部署（推荐）

1. **创建GitHub仓库**
   - 登录GitHub
   - 创建新仓库，命名为`christmas-tree-interactive-album`
   - 将代码推送到仓库

2. **启用GitHub Pages**
   - 进入仓库Settings
   - 找到Pages设置
   - 选择GitHub Actions作为部署源

3. **自动部署**
   - 每次推送到main分支都会自动部署
   - 部署状态可在Actions标签页查看

### 方法2：手动部署到gh-pages分支

1. **安装gh-pages**
   ```bash
   npm install --save-dev gh-pages
   ```

2. **构建并部署**
   ```bash
   npm run deploy:gh
   ```

3. **配置GitHub Pages**
   - 进入仓库Settings > Pages
   - 选择Deploy from a branch
   - 选择gh-pages分支和/(root)目录

## ⚙️ 配置文件说明

### GitHub Actions工作流 (`.github/workflows/deploy-github-pages.yml`)
- 自动构建React应用
- 部署到GitHub Pages
- 支持手动触发部署

### Vite配置 (`vite.config.github.js`)
- 专门为GitHub Pages优化
- 正确的base路径配置
- 代码分割和压缩

## 🎯 部署后访问

部署完成后，您的应用将在以下地址访问：
```
https://[your-username].github.io/christmas-tree-interactive-album/
```

## 🔧 常见问题解决

### 问题1：页面空白或404错误
- 检查仓库是否设置为公开
- 确认GitHub Pages已启用
- 检查构建是否成功

### 问题2：资源加载失败
- 检查Vite的base路径配置
- 确认文件是否正确上传到gh-pages分支
- 检查浏览器控制台错误信息

### 问题3：构建失败
- 检查Node.js版本
- 确认依赖安装完整
- 查看GitHub Actions日志

## 📊 部署状态检查

1. **查看部署状态**
   - 进入GitHub仓库的Actions标签页
   - 查看最新的部署工作流运行状态

2. **访问部署地址**
   - 等待部署完成（通常2-3分钟）
   - 访问提供的GitHub Pages链接

3. **验证功能**
   - 检查3D场景是否正常加载
   - 测试照片展示功能
   - 验证音乐播放
   - 检查移动端适配

## 🚀 快速开始

如果您还没有GitHub仓库，按照以下步骤操作：

```bash
# 1. 初始化git仓库
git init

# 2. 添加远程仓库
git remote add origin https://github.com/YOUR_USERNAME/christmas-tree-interactive-album.git

# 3. 推送代码
git add .
git commit -m "Initial commit"
git push -u origin main

# 4. 启用GitHub Pages后，手动触发部署
npm run deploy:gh
```

## 🎉 恭喜！

几分钟后，您的圣诞互动相册就会上线，可以通过GitHub Pages链接访问了！

**访问地址格式**：
```
https://[your-github-username].github.io/christmas-tree-interactive-album/
```

现在就可以分享这个温馨的圣诞互动相册给朋友们了！🎄✨